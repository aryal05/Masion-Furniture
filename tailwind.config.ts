// Tailwind CSS v4 — configuration is done via @theme in globals.css
// This file is intentionally minimal; v4 uses CSS-first configuration.
// See: https://tailwindcss.com/docs/v4-beta

import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
  theme: {},
  plugins: [],
};

export default config;
